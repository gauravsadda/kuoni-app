import { Typography } from "@mui/material";

const AuthorableSection = ({}) => {
  return (
    <>
      <Typography>This is the AuthorableSection</Typography>
    </>
  );
};

export default AuthorableSection;
