import { Typography } from "@mui/material";

const AccordionAuthorableSection = ({}) => {
  return (
    <>
      <Typography>This is the accordion authorable section</Typography>
    </>
  );
};

export default AccordionAuthorableSection;
